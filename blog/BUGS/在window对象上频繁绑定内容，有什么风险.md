# 在 window 对象上频繁绑定内容，有什么风险

## 风险分析

1. 命名冲突。`window.a = "hello"`
2. 全局污染。
3. 安全风险。任何人都能通过`window.a`访问这个变量
4. 性能问题，增加内存开销

## 解决方案

1. 模块化（commonjs、amd、esmodule）
2. 命名空间
3. 立即执行函数（IIFE），形成闭包，形成独立作用域
4. 开启严格模式

了解下 qiankun（微前端），快照沙箱等实现原理？？？
