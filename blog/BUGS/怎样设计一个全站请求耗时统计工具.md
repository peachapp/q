# 怎样设计一个全站请求耗时统计工具

## 背景：

通过这个统计工具，可以更清晰地看到整个站点性能情况，例如首屏加载时间（FP/FCP）

1. 监控请求耗时：HTTP、中间件，axios
2. 前端监控：监控整个请求相关的，并且记录耗时数据
3. 后端监控：后端记录
4. 数据汇总：数据清洗加工，数据可视化，可视化图表

## 解决方案：

前端监控：

```js
(function () {
  const originalXhrOpen = XMLHttpRequest.prototype.open;
  XMLHttpRequest.prototype.open = function (...args) {
    this._startTime = performance.now();
    this.addEventListener("load", function () {
      const duration = performance.now() - this._startTime;
      console.log(`XHR ${args[1]} took ${duration}ms`);
      reportRequestDuration(args[1], duration, "XHR");
    });
    originalXhrOpen.apply(this, args);
  };

  const originalFetch = window.fetch;
  window.fetch = async function (...args) {
    const startTime = performance.now();
    const response = await originalFetch.apply(this, args);
    const duration = performance.now() - startTime;
    console.log(`Fetch ${args[0]} took ${duration}ms`);
    reportRequestDuration(args[0], duration, "Fetch");
    return response;
  };

  // fetch 上报
  function reportRequestDuration(url, duration, type) {
    //  Report to backend or log it locally
    fetch("/api/report-request-duration", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ url, duration, type }),
    });
  }

  // 也可以通过图片上报
  /*
  const img = document.createElement("img");
  img.src = "/api/report-request-duration?a=1&b=2"; // 上报地址
  document.body.appendChild(img);
  */
})();
```

后端监控：

```js
const express = require("express");
const app = express();

app.use((req, res, next) => {
  const startHrTime = process.hrtime();
  res.on("finish", () => {
    const elapsedHrTime = process.hrtime();
    const elapsedtimeInMs = elapsedHrTime[0] * 1000 + elapsedHrTime[1] / 1e6;
    console.log(`${req.method} ${req.url} took ${elapsedTimeInMs}ms`);
    reportRequestDuration(req.url, elapsedTimeInMs, "Backend");
  });
  next();
});

app.post("/api/report-request-duration", express.json(), (req, res) => {
  // Save the request duration data to the database
  console.log("Received duration data:", req.body);
  res.sendStatus(200);
});

// Your other routes go here

app.listen(3000, () => {
  console.log("Server is running on port 3000");
});

function ReportRequestDuration(url, duration, type) {
  // Save the request duration data to the database
  // This function can ba used to aggregate and save the data
}
```

数据汇总：

```sql
CREATE TABLE request_durations (
  id INT AUTO_INCREMENT PRIMARY KEY,
  url VARCHAR(255),
  duration FLOAT,
  type ENUM("XHR","Fetch","Backend"),
  timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

生成报告：

```js
const mysql = require("mysql");
const connection = mysql.createConnection({
  host: "localhost",
  user: "root",
  password: "password",
  database: "performance_monitoring",
});

function generateReport() {
  const query = `
    SELECT 
        url,
        AVG(duration) as avg_duration,
        MAX(duration) as max_duration,
        MIN(duration) as min_duration,
        COUNT(*) as request_count
    FROM request_durations
    GROUP BY url, type
  `;

  connection.query(query, (error, results) => {
    if (error) throw error;
    console.log("Performance Report：", results);
    // Further processing or saving the report
  });
}

// Schedule the report generation (e.g., daily)
setInterval(generateReport, 24 * 60 * 60 * 1000);

// Start the process
generateReport();
```

### 总结：

做这个上报工具设计的时候涉及到性能监控的 SDK 和性能监控平台两部分。SDK 负责整个数据的上报，监控平台负责数据处理，处理完之后把数据落库，落库之后做可视化。
