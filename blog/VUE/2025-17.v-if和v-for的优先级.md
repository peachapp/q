# 2025-17.v-if 和 v-for 的优先级

v-for 和 v-if 避免在同一个标签中使用。如果遇到需要同时使用时可以考虑写成计算属性的方式。

```html
<!-- 应当避免这种写法 -->
<li v-for="l in arr" v-if="exists"></li>
```

- 在 vue2 中解析时，先解析 v-for，再解析 v-if，会导致先循环后再对每一项进行判断，浪费性能。
- 在 vue3 中，v-if 的优先级高于 v-for。
